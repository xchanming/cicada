type MediaUploadResult = {
    failureAmount: number;
    successAmount: number;
    totalAmount: number;
    targetId: string;
};

/**
 * @private
 * @package discovery
 */
export default MediaUploadResult;
